/*!
 * Module dependencies.
 */

function sendLogEntries(logEntries, host, port, apiKey) {
    // using rest-client, send log entries to server in bulk
}

/*!
 * Module exports.
 */
module.exports = {
    submitLogEntries: sendLogEntries
};
